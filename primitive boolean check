function booWho(bool) {
  // A check to see if the value is boolean or not using typeof
  if (typeof bool === 'boolean') {
    return true;
  } else {
    return false;
 }
 // commented out ternary version, that does work!!
 //return (typeof bool==='boolean' ? true : false);
}

booWho(null);
