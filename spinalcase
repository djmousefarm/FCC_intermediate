function spinalCase(str) {
  // "It's such a fine line between stupid, and clever."
  // --David St. Hubbins
  return str.toLowerCase().replace(/[ ]/g,function() {return '-';});
  // ([A-Z ])\s\B
}

spinalCase('This Is Spinal Tap');
